# Server port. Render injects PORT automatically in production.
PORT=5454

# MongoDB connection string (local, Docker, or MongoDB Atlas).
MONGODB_URI=mongodb+srv://shahsujal2512_db_user:M16cvIQlI5AnQSc5@shikaku-game.js9pnuc.mongodb.net/shikaku?retryWrites=true&w=majority

# development | production | test
NODE_ENV=development

# Comma-separated extra origins allowed for CORS / Socket.IO.
# The bundled frontend is same-origin and works without this.
CLIENT_URL=http://localhost:3001

# Optional: fatal | error | warn | info | debug | trace | silent
# LOG_LEVEL=info
